    data.forEach  
    
    function getActiveYears {
      yearsActive = element.year > 0
      // this will be an array
    }
    function getFirstYear {
      firstYear = yearsActive lowest integer
      // look in the array and get lowest integer
    }
    
    function getLastYear {
      lastYear = yearsActive highest integer
      // look in the array and get highest integer
    }
    
    function getCurrentYear {
      CurrentYear =
      // probably an existing mod to get current date
      // isolate year
    }
    
        
        var year = element.year
        
        var startYear = first year with > null 
        var endYear = last year with > null 
        var nowYear = get current date, isolate year
        var activeProject = true if a project starts =< current year
        var 
        
        var totalSpent = sum of acive projects content
        var totalActive = sum of number of acive projects
        
        
         function getEd(projects) {
            var isEd = false
            projects.forEach(function (project) {
              var type = "type"
              var pojectType = project[type]
              if (projectType == "Economic Development") isEd = true
            })
            return isEd
            console.log(isEd)
          }
        
          
          
            function getProjectType() {
              var projectType = ""
              projects.forEach(function getType(element) {
                if ( element[type] == "Economic Development") projectType = "isEd"
                if ( element[type] == "Rec & Cultural Arts") projectType = isRa = true
                if ( element[type] == "Debt Retirement") isDr = true
                if ( element[type] == "Public Safety") isPs = true  
              })
              return isEd  
            }
            console.log(isEd)
            
            
            function getEd(element) {
                var isEd = false
                var type = "type"
                var pojectType = element[type]
                if (projectType == "Economic Development") isEd = true
                return isEd
                console.log(isEd)
              }